```javascript
const {
  Document, Packer, Paragraph, TextRun, Table, TableRow, TableCell,
  HeadingLevel, AlignmentType, BorderStyle, WidthType, ShadingType,
  PageNumber, PageBreak, LevelFormat, UnderlineType
} = require('docx');
const fs = require('fs');

// ── 颜色常量 ──────────────────────────────────────────────
const C = {
  primary:   '1A56DB',  // 蓝
  secondary: '1E429F',  // 深蓝
  accent:    '0E9F6E',  // 绿
  warning:   'C27803',  // 橙
  danger:    'C81E1E',  // 红
  light:     'EBF5FB',  // 浅蓝背景
  lightGray: 'F3F4F6',  // 浅灰背景
  midGray:   'D1D5DB',  // 边框灰
  darkGray:  '374151',  // 正文深灰
  white:     'FFFFFF',
};

// ── 边框辅助 ──────────────────────────────────────────────
function border(color = C.midGray, size = 4) {
  return { style: BorderStyle.SINGLE, size, color };
}
function allBorders(color = C.midGray, size = 4) {
  const b = border(color, size);
  return { top: b, bottom: b, left: b, right: b };
}
function noBorder() {
  const b = { style: BorderStyle.NONE, size: 0, color: 'FFFFFF' };
  return { top: b, bottom: b, left: b, right: b };
}

// ── 段落辅助 ──────────────────────────────────────────────
function h1(text) {
  return new Paragraph({
    heading: HeadingLevel.HEADING_1,
    spacing: { before: 360, after: 120 },
    children: [new TextRun({ text, bold: true, size: 36, color: C.secondary, font: 'Arial' })],
  });
}
function h2(text) {
  return new Paragraph({
    heading: HeadingLevel.HEADING_2,
    spacing: { before: 280, after: 100 },
    children: [new TextRun({ text, bold: true, size: 28, color: C.primary, font: 'Arial' })],
  });
}
function h3(text) {
  return new Paragraph({
    heading: HeadingLevel.HEADING_3,
    spacing: { before: 200, after: 80 },
    children: [new TextRun({ text, bold: true, size: 24, color: C.darkGray, font: 'Arial' })],
  });
}
function p(text, opts = {}) {
  return new Paragraph({
    spacing: { before: 60, after: 60 },
    children: [new TextRun({ text, size: 22, color: C.darkGray, font: 'Arial', ...opts })],
  });
}
function blank() {
  return new Paragraph({ spacing: { before: 40, after: 40 }, children: [new TextRun('')] });
}
function rule() {
  return new Paragraph({
    spacing: { before: 120, after: 120 },
    border: { bottom: { style: BorderStyle.SINGLE, size: 4, color: C.midGray } },
    children: [new TextRun('')],
  });
}
function bullet(text, level = 0) {
  return new Paragraph({
    numbering: { reference: 'bullets', level },
    spacing: { before: 40, after: 40 },
    children: [new TextRun({ text, size: 22, color: C.darkGray, font: 'Arial' })],
  });
}
function numbered(text, level = 0) {
  return new Paragraph({
    numbering: { reference: 'numbers', level },
    spacing: { before: 40, after: 40 },
    children: [new TextRun({ text, size: 22, color: C.darkGray, font: 'Arial' })],
  });
}

// ── 代码块（灰底等宽）───────────────────────────────────────
function code(text) {
  return new Paragraph({
    spacing: { before: 40, after: 40 },
    shading: { fill: C.lightGray, type: ShadingType.CLEAR },
    indent: { left: 360, right: 360 },
    children: [new TextRun({ text, size: 18, font: 'Courier New', color: '1F2937' })],
  });
}

// ── 信息框（蓝底提示）────────────────────────────────────────
function infoBox(label, lines) {
  const children = [
    new TextRun({ text: label + '  ', bold: true, size: 22, color: C.primary, font: 'Arial' }),
    new TextRun({ text: lines, size: 22, color: C.darkGray, font: 'Arial' }),
  ];
  return new Paragraph({
    spacing: { before: 80, after: 80 },
    indent: { left: 360, right: 360 },
    shading: { fill: C.light, type: ShadingType.CLEAR },
    border: { left: { style: BorderStyle.SINGLE, size: 16, color: C.primary } },
    children,
  });
}

// ── 通用表格 ──────────────────────────────────────────────
function makeTable(headers, rows, colWidths) {
  const totalW = colWidths.reduce((a, b) => a + b, 0);
  const headerRow = new TableRow({
    tableHeader: true,
    children: headers.map((h, i) =>
      new TableCell({
        width: { size: colWidths[i], type: WidthType.DXA },
        borders: allBorders(C.primary, 6),
        shading: { fill: C.primary, type: ShadingType.CLEAR },
        margins: { top: 80, bottom: 80, left: 160, right: 160 },
        children: [new Paragraph({
          alignment: AlignmentType.CENTER,
          children: [new TextRun({ text: h, bold: true, size: 20, color: C.white, font: 'Arial' })],
        })],
      })
    ),
  });
  const dataRows = rows.map((row, ri) =>
    new TableRow({
      children: row.map((cell, ci) =>
        new TableCell({
          width: { size: colWidths[ci], type: WidthType.DXA },
          borders: allBorders(C.midGray, 4),
          shading: { fill: ri % 2 === 0 ? C.white : C.lightGray, type: ShadingType.CLEAR },
          margins: { top: 80, bottom: 80, left: 160, right: 160 },
          children: [new Paragraph({
            children: [new TextRun({ text: String(cell), size: 20, color: C.darkGray, font: 'Arial' })],
          })],
        })
      ),
    })
  );
  return new Table({
    width: { size: totalW, type: WidthType.DXA },
    columnWidths: colWidths,
    rows: [headerRow, ...dataRows],
  });
}

// ══════════════════════════════════════════════════════════
// 文档内容
// ══════════════════════════════════════════════════════════

const doc = new Document({
  numbering: {
    config: [
      { reference: 'bullets', levels: [
          { level: 0, format: LevelFormat.BULLET, text: '•', alignment: AlignmentType.LEFT,
            style: { paragraph: { indent: { left: 720, hanging: 360 } } } },
          { level: 1, format: LevelFormat.BULLET, text: '◦', alignment: AlignmentType.LEFT,
            style: { paragraph: { indent: { left: 1080, hanging: 360 } } } },
        ],
      },
      { reference: 'numbers', levels: [
          { level: 0, format: LevelFormat.DECIMAL, text: '%1.', alignment: AlignmentType.LEFT,
            style: { paragraph: { indent: { left: 720, hanging: 360 } } } },
          { level: 1, format: LevelFormat.DECIMAL, text: '%2.', alignment: AlignmentType.LEFT,
            style: { paragraph: { indent: { left: 1080, hanging: 360 } } } },
        ],
      },
    ],
  },
  styles: {
    default: { document: { run: { font: 'Arial', size: 22 } } },
    paragraphStyles: [
      { id: 'Heading1', name: 'Heading 1', basedOn: 'Normal', next: 'Normal', quickFormat: true,
        run: { size: 36, bold: true, font: 'Arial', color: C.secondary },
        paragraph: { spacing: { before: 360, after: 120 }, outlineLevel: 0 } },
      { id: 'Heading2', name: 'Heading 2', basedOn: 'Normal', next: 'Normal', quickFormat: true,
        run: { size: 28, bold: true, font: 'Arial', color: C.primary },
        paragraph: { spacing: { before: 280, after: 100 }, outlineLevel: 1 } },
      { id: 'Heading3', name: 'Heading 3', basedOn: 'Normal', next: 'Normal', quickFormat: true,
        run: { size: 24, bold: true, font: 'Arial', color: C.darkGray },
        paragraph: { spacing: { before: 200, after: 80 }, outlineLevel: 2 } },
    ],
  },
  sections: [{
    properties: {
      page: {
        size: { width: 11906, height: 16838 },
        margin: { top: 1440, right: 1440, bottom: 1440, left: 1440 },
      },
    },
    children: [

      // ── 封面 ─────────────────────────────────────────────
      new Paragraph({ spacing: { before: 1440, after: 40 }, alignment: AlignmentType.CENTER,
        children: [new TextRun({ text: 'bnsy-operator', size: 56, bold: true, color: C.primary, font: 'Arial' })] }),
      new Paragraph({ spacing: { before: 0, after: 40 }, alignment: AlignmentType.CENTER,
        children: [new TextRun({ text: '笨鸟速运网点操作中心自动化系统', size: 36, color: C.secondary, font: 'Arial' })] }),
      new Paragraph({ spacing: { before: 0, after: 40 }, alignment: AlignmentType.CENTER,
        children: [new TextRun({ text: '开发规格文档  v1.0', size: 24, color: '6B7280', font: 'Arial' })] }),
      new Paragraph({ spacing: { before: 0, after: 40 }, alignment: AlignmentType.CENTER,
        children: [new TextRun({ text: '雨图饰品 · 内部工具  |  2026-06-18', size: 20, color: '9CA3AF', font: 'Arial' })] }),
      blank(), blank(),
      rule(),
      blank(),

      // ══════════════════════════════════════════════════
      // 第一章  项目概述
      // ══════════════════════════════════════════════════
      h1('1  项目概述'),

      h2('1.1  背景'),
      p('雨图饰品旗下笨鸟速运在天津运营两个网点：天南大公司（天津大学 / 南开大学区域）和和苑公司（红桥区域）。两个网点每日处理 5,000 ~ 10,000 票快件，日常操作包括到件扫描、派件扫描、签收录入等，目前全部依赖人工在凤凰系统（bnsy.benniaosuyun.com）逐票手动完成，效率低且易出错。'),
      p('本项目通过 Playwright CDP 接入已登录的 EasyBR 指纹浏览器窗口，以程序驱动方式完成批量操作，并提供简洁的 Web 操作界面，使非技术员工也能轻松使用。'),
      blank(),

      h2('1.2  目标'),
      bullet('到件扫描：批量提交，500 单 / 批，替代手工逐票操作'),
      bullet('派件扫描：多快递员窗口并发，显著压缩派件操作时间'),
      bullet('签收录入：按快递员查询未签收件，一键批量签收'),
      bullet('统一操作界面：Web UI，本地访问，任何人均可使用'),
      bullet('操作日志：每次任务完整记录，可追溯、可导出'),
      blank(),

      h2('1.3  范围说明（本期不含）'),
      bullet('问题件 / 工单飞书监控通知'),
      bullet('快递员区域自动分配'),
      bullet('签收数据回传或对账'),
      blank(),
      rule(),

      // ══════════════════════════════════════════════════
      // 第二章  系统架构
      // ══════════════════════════════════════════════════
      h1('2  系统架构'),

      h2('2.1  整体结构'),
      p('系统完全运行在本地 Windows 机器上，通过 Docker Desktop 容器化部署，无需公网服务器。'),
      blank(),
      infoBox('运行环境', 'Windows 本地机器 · Docker Desktop · EasyBR 指纹浏览器（已登录）'),
      blank(),

      makeTable(
        ['层次', '组件', '说明'],
        [
          ['浏览器层', 'EasyBR 多窗口', '管理员账号 + 员工账号，已登录凤凰系统，各自持有 CDP 调试端口'],
          ['连接层', 'BrowserPool', 'Playwright connectOverCDP，管理所有窗口连接，自动识别角色'],
          ['操作层', 'Operations', '到件 / 派件 / 签收三个操作模块，封装页面交互逻辑'],
          ['数据层', 'SQLite', '存储窗口配置、任务记录、操作结果'],
          ['接口层', 'Express API', 'REST 接口，前端与后端通信'],
          ['前端层', 'React SPA', '单页应用，由 Express 静态托管，浏览器访问 localhost:3100'],
        ],
        [1400, 2200, 5000]
      ),
      blank(),

      h2('2.2  EasyBR 窗口与网点映射'),
      p('系统启动时调用 EasyBR 本地 API（http://127.0.0.1:3001/auto/openedList）获取所有已开启的窗口列表，按命名规则自动分类：'),
      blank(),
      makeTable(
        ['窗口命名格式', '类型', '示例', '用途'],
        [
          ['不含"-"', '管理员窗口', '天南大公司 / 和苑公司', '到件扫描（必须管理员）'],
          ['含"-"，格式：网点-姓名', '员工窗口', '天南大-张三 / 和苑-李四', '派件扫描（并发执行）'],
        ],
        [2200, 1600, 2400, 2400]
      ),
      blank(),
      p('CDP 端口通过扫描本机 9200~20000 端口范围，对每个端口请求 /json/version 进行识别，将发现的端口与窗口名称关联存入数据库。'),
      blank(),

      h2('2.3  部署结构'),
      blank(),
      code('本地 Windows 机器'),
      code('├── EasyBR（常驻运行，已登录所有网点窗口）'),
      code('│   ├── 天南大公司  CDP:17670  （管理员）'),
      code('│   ├── 和苑公司    CDP:10396  （管理员）'),
      code('│   ├── 天南大-张三  CDP:XXXX  （员工）'),
      code('│   └── 天南大-李四  CDP:XXXX  （员工）'),
      code('│'),
      code('└── Docker Desktop'),
      code('    └── bnsy-operator 容器'),
      code('        ├── Express 后端  :3100'),
      code('        ├── React 前端（静态托管）'),
      code('        └── SQLite 数据库（挂载到本地 ./data/ 目录）'),
      blank(),
      infoBox('访问方式', '浏览器打开 http://localhost:3100 即可使用，无需安装任何客户端软件'),
      blank(),
      rule(),

      // ══════════════════════════════════════════════════
      // 第三章  后端规格
      // ══════════════════════════════════════════════════
      h1('3  后端规格'),

      h2('3.1  目录结构'),
      blank(),
      code('src/'),
      code('├── browser/'),
      code('│   ├── BrowserPool.ts        # CDP连接池，管理所有EasyBR窗口'),
      code('│   └── PageNavigator.ts      # 页面导航、等待、截图封装'),
      code('├── operations/'),
      code('│   ├── BaseOperation.ts      # 操作基类，定义OperationResult接口'),
      code('│   ├── ArriveScanBatch.ts    # 到件扫描（批量textarea模式）'),
      code('│   ├── DispatchScan.ts       # 派件扫描（逐条+多窗口并发）'),
      code('│   └── SignReceipt.ts        # 签收录入（查询+批量确认）'),
      code('├── parser/'),
      code('│   └── FileParser.ts         # 文件解析：xlsx/csv/txt'),
      code('├── db/'),
      code('│   └── Database.ts           # SQLite封装，三张表'),
      code('├── api/'),
      code('│   └── routes.ts             # Express路由定义'),
      code('└── index.ts                  # 入口，启动服务'),
      blank(),

      h2('3.2  BrowserPool 连接池'),
      p('负责维护所有 EasyBR 窗口的 CDP 连接，是整个系统的基础。'),
      blank(),
      h3('初始化流程'),
      numbered('调用 EasyBR API GET /auto/openedList，获取所有已开启窗口的 browerid 和 browername'),
      numbered('扫描本机端口，发现所有 Chromium CDP 调试端口（请求 /json/version 验证）'),
      numbered('在每个 CDP 端口下，查找 URL 包含 bnsy.benniaosuyun.com 的标签页，关联到对应窗口'),
      numbered('按命名规则分类：无"-"→ 管理员窗口，含"-"→ 员工窗口（解析网点和姓名）'),
      numbered('所有连接信息持久化到 SQLite windows 表'),
      blank(),
      h3('对外接口'),
      bullet('getAdminPage(site): Promise<Page>  — 获取指定网点管理员窗口的页面对象'),
      bullet('getStaffPage(staffName): Promise<Page>  — 获取指定员工窗口的页面对象'),
      bullet('listWindows(): WindowInfo[]  — 获取所有窗口状态列表'),
      bullet('reconnect(windowId)  — 单个窗口重连（处理断线场景）'),
      blank(),
      infoBox('重要约束', '所有 CDP 操作复用已有标签页，不新建也不关闭标签页，避免破坏 EasyBR 的登录会话状态'),
      blank(),

      h2('3.3  操作模块详细规格'),

      h3('3.3.1  到件扫描（ArriveScanBatch）'),
      p('使用批量 textarea 模式，500 单一批，由管理员账号操作。'),
      blank(),
      makeTable(
        ['属性', '值'],
        [
          ['操作账号', '管理员窗口（天南大公司 / 和苑公司）'],
          ['目标页面', '/scanning/ArrivalscanBatch'],
          ['输入方式', 'textarea[placeholder="运单号"] 填入换行分隔的单号'],
          ['触发按钮', '"批量到件"按钮'],
          ['批次大小', '500 单 / 批（可配置）'],
          ['批次间隔', '等待上一批系统响应完成后再提交下一批'],
          ['结果获取', '等待 .el-message 或 .el-notification 出现，读取文字内容'],
        ],
        [2400, 6200]
      ),
      blank(),
      h3('执行流程'),
      numbered('导航到 /scanning/ArrivalscanBatch，等待 textarea 出现'),
      numbered('将当前批次单号用换行符拼接，填入 textarea'),
      numbered('点击"批量到件"按钮'),
      numbered('等待 toast 提示出现（超时 15 秒），记录提示内容'),
      numbered('清空 textarea，继续下一批'),
      numbered('全部批次完成，汇总成功 / 失败数量，更新任务状态'),
      blank(),

      h3('3.3.2  派件扫描（DispatchScan）'),
      p('每个快递员独立窗口，并发执行，逐条输入，操作前先选定快递员。'),
      blank(),
      makeTable(
        ['属性', '值'],
        [
          ['操作账号', '员工窗口（命名格式：网点-姓名）'],
          ['目标页面', '/scanning/dispatchscan'],
          ['操作模式', '逐条输入：每条单号填入输入框后模拟回车'],
          ['并发方式', 'Promise.all，各窗口同时独立运行，互不干扰'],
          ['快递员选择', '页面进入后先操作下拉框选定快递员，全程不重新选择'],
          ['结果获取', '每条单号回车后等待 toast，读取成功/失败信息'],
          ['超时设置', '单条单号操作超时 5 秒，超时记为失败继续下一条'],
        ],
        [2400, 6200]
      ),
      blank(),
      h3('执行流程（单个窗口）'),
      numbered('接收任务：{ staffName: "张三", waybillNos: ["75001", "75002", ...] }'),
      numbered('导航到 /scanning/dispatchscan，等待页面就绪'),
      numbered('找到快递员下拉框，搜索并选择与 staffName 匹配的选项'),
      numbered('确认下拉已选中正确快递员'),
      numbered('循环处理每个运单号：'),
      bullet('找到运单号输入框 input[placeholder*="请输入运单号"]', 1),
      bullet('填入运单号，触发 keyboard.press("Enter")', 1),
      bullet('等待 toast 出现，记录结果', 1),
      bullet('输入框清空（部分系统会自动清空，否则手动清空）', 1),
      numbered('全部完成，上报结果到任务调度器'),
      blank(),
      infoBox('并发说明', '多个快递员的任务通过 Promise.all 同时启动，各自操作独立的浏览器窗口，瓶颈为单量最多的那个快递员'),
      blank(),

      h3('3.3.3  签收录入（SignReceipt）'),
      p('通过筛选查询出未签收件，全选后批量签收，管理员账号即可操作，无需员工窗口。'),
      blank(),
      makeTable(
        ['属性', '值'],
        [
          ['操作账号', '管理员窗口'],
          ['目标页面', '/scanning/signFor/signForInput'],
          ['操作模式', '查询 → 全选 → 批量签收（非逐条扫描）'],
          ['筛选条件', '时间范围（默认当天）+ 派件员姓名（下拉选择）'],
          ['多快递员', '循环处理每个快递员，每次签收完再切换下一个'],
        ],
        [2400, 6200]
      ),
      blank(),
      h3('执行流程（单个快递员）'),
      numbered('导航到 /scanning/signFor/signForInput'),
      numbered('设置签收时间范围（填写开始日期和结束日期）'),
      numbered('在"派件员"下拉中选择目标快递员姓名'),
      numbered('点击"搜索"按钮，等待结果加载'),
      numbered('读取结果条数，若为 0 则跳过该快递员'),
      numbered('点击全选 checkbox，勾选所有行'),
      numbered('点击"批量签收"按钮，等待确认弹窗'),
      numbered('确认弹窗，等待操作完成提示'),
      numbered('记录该快递员签收条数，继续处理下一个快递员'),
      blank(),

      h2('3.4  文件解析模块（FileParser）'),
      p('支持三种输入方式，统一输出 WaybillRecord[] 格式。'),
      blank(),
      makeTable(
        ['输入类型', '解析方式', '说明'],
        [
          ['.xlsx / .xls', 'xlsx 库解析', '读取第一个 Sheet，展示所有列名供用户选择运单号列'],
          ['.csv', 'papaparse 解析', '自动检测分隔符，展示列名供用户确认'],
          ['.txt', '正则提取', '从文本中提取所有符合运单号规则的数字串（长度 12~15 位）'],
          ['手动粘贴', '正则提取', '同 .txt，支持换行/逗号/空格混合分隔'],
        ],
        [1800, 2200, 4600]
      ),
      blank(),
      h3('运单号自动识别规则'),
      bullet('纯数字串，长度 12~15 位'),
      bullet('或以 55 / 75 / 76 / 77 开头的数字串'),
      bullet('同一行出现多个候选时，优先选择最长的一个'),
      blank(),
      h3('列识别优先级（Excel/CSV）'),
      p('系统按以下关键词依次匹配列名，命中即自动选中：'),
      bullet('"运单号" "快递单号" "单号" "waybill" "tracking"  → 运单号列'),
      bullet('"收件人" "姓名" "name" "收件姓名"  → 收件人列（可选）'),
      bullet('"地址" "收件地址" "收货地址" "address"  → 地址列（可选）'),
      blank(),

      h2('3.5  数据库设计（SQLite）'),
      blank(),
      h3('windows 表 — EasyBR 窗口配置'),
      makeTable(
        ['字段', '类型', '说明'],
        [
          ['id', 'TEXT PK', 'EasyBR browerid'],
          ['name', 'TEXT', '窗口名称（如"天南大公司"/"天南大-张三"）'],
          ['cdp_port', 'INTEGER', 'CDP 调试端口'],
          ['role', 'TEXT', 'admin / staff'],
          ['site', 'TEXT', 'tiannanda / heyuan'],
          ['staff_name', 'TEXT', '员工姓名（仅员工窗口）'],
          ['is_connected', 'INTEGER', '1=已连接 0=断线'],
          ['updated_at', 'TEXT', '最后更新时间'],
        ],
        [1800, 1400, 5400]
      ),
      blank(),
      h3('tasks 表 — 任务记录'),
      makeTable(
        ['字段', '类型', '说明'],
        [
          ['id', 'TEXT PK', 'UUID'],
          ['type', 'TEXT', 'arrive / dispatch / sign'],
          ['site', 'TEXT', '操作网点'],
          ['status', 'TEXT', 'pending / running / done / failed'],
          ['total_count', 'INTEGER', '总单量'],
          ['done_count', 'INTEGER', '已处理数'],
          ['fail_count', 'INTEGER', '失败数'],
          ['input_data', 'TEXT', '输入数据 JSON'],
          ['result_data', 'TEXT', '结果明细 JSON'],
          ['created_at', 'TEXT', '创建时间'],
          ['finished_at', 'TEXT', '完成时间'],
        ],
        [2000, 1200, 5400]
      ),
      blank(),

      h2('3.6  HTTP API 接口'),
      blank(),
      makeTable(
        ['Method', 'Path', '说明', '请求体 / 参数'],
        [
          ['GET', '/api/status', '所有窗口连接状态', '—'],
          ['GET', '/api/windows', '窗口列表（含角色、网点、连接状态）', '—'],
          ['POST', '/api/operations/arrive', '提交到件任务', '{ site, waybillNos[] }'],
          ['POST', '/api/operations/dispatch', '提交派件任务', '{ assignments: [{staffName, waybillNos[]}] }'],
          ['POST', '/api/operations/sign', '提交签收任务', '{ site, staffNames[], dateRange }'],
          ['GET', '/api/operations/:taskId', '查询任务进度和结果', '—'],
          ['GET', '/api/operations/history', '历史任务列表', '?page&limit&type'],
        ],
        [700, 2400, 2800, 2700]
      ),
      blank(),
      h3('任务异步模型'),
      p('所有操作任务均为异步执行。前端提交任务后立即获得 taskId，通过轮询 /api/operations/:taskId 获取实时进度。'),
      blank(),
      code('// 提交响应'),
      code('{ "taskId": "uuid-xxx", "status": "pending" }'),
      blank(),
      code('// 轮询响应（每3秒一次）'),
      code('{ "taskId": "uuid-xxx",'),
      code('  "status": "running",'),
      code('  "progress": { "done": 230, "total": 1000 },'),
      code('  "results": ['),
      code('    { "waybillNo": "75001234567", "success": true,  "message": "到件成功" },'),
      code('    { "waybillNo": "75001234568", "success": false, "message": "运单不存在" }'),
      code('  ]'),
      code('}'),
      blank(),
      rule(),

      // ══════════════════════════════════════════════════
      // 第四章  前端规格
      // ══════════════════════════════════════════════════
      h1('4  前端规格'),

      h2('4.1  技术选型'),
      makeTable(
        ['技术', '选择', '原因'],
        [
          ['框架', 'React + Vite', '轻量，构建产物由 Express 静态托管，无需独立服务'],
          ['样式', 'Tailwind CSS', '快速布局，无需维护 CSS 文件'],
          ['文件解析', 'xlsx + papaparse', '浏览器端直接解析上传文件，无需上传到服务器'],
          ['状态管理', 'React useState/useReducer', '无需 Redux，状态简单'],
          ['HTTP 客户端', 'fetch API', '原生支持，无需额外依赖'],
        ],
        [1400, 2200, 5000]
      ),
      blank(),

      h2('4.2  页面结构'),
      blank(),
      code('┌─────────────────────────────────────────────────────┐'),
      code('│  顶部状态栏                                          │'),
      code('│  天南大公司 🟢  和苑公司 🟢                          │'),
      code('├──────────────┬──────────────────────────────────────┤'),
      code('│              │                                      │'),
      code('│  📦 到件扫描  │                                      │'),
      code('│              │           主内容区                    │'),
      code('│  🚚 派件扫描  │                                      │'),
      code('│              │                                      │'),
      code('│  ✅ 签收录入  │                                      │'),
      code('│              │                                      │'),
      code('│  📋 操作日志  │                                      │'),
      code('└──────────────┴──────────────────────────────────────┘'),
      blank(),

      h2('4.3  数据输入组件（共用）'),
      p('到件扫描和派件扫描均使用此组件作为单号输入入口，签收无需输入单号。'),
      blank(),
      h3('Tab 1：文件上传'),
      numbered('用户拖拽或点击上传文件（.xlsx / .xls / .csv / .txt）'),
      numbered('前端解析文件，展示列名列表和前 3 行数据预览'),
      numbered('系统自动猜测运单号列（高亮显示），用户可手动修改'),
      numbered('可选：指定收件人列、收件地址列'),
      numbered('点击"确认"，系统提取运单号和附加信息，显示识别数量'),
      blank(),
      h3('Tab 2：手动粘贴'),
      numbered('大文本框，支持换行 / 逗号 / 空格混合格式'),
      numbered('实时识别有效运单号数量，显示在文本框下方'),
      numbered('点击"确认"完成输入'),
      blank(),

      h2('4.4  到件扫描页面'),
      blank(),
      makeTable(
        ['区域', '内容'],
        [
          ['网点选择', '下拉框：天南大公司 / 和苑公司'],
          ['数据输入', '文件上传 / 手动粘贴（共用组件）'],
          ['识别结果', '显示"已识别 N 条运单号，跳过 M 条无效行"'],
          ['执行按钮', '"开始到件扫描"，点击后变灰并显示进度'],
          ['进度显示', '进度条 + 已处理/总量数字 + 成功/失败计数'],
          ['失败明细', '实时展示失败的运单号和失败原因'],
        ],
        [2000, 6600]
      ),
      blank(),

      h2('4.5  派件扫描页面'),
      blank(),
      makeTable(
        ['区域', '内容'],
        [
          ['网点选择', '下拉框，过滤出该网点的员工窗口列表'],
          ['快递员分配', '动态表格，每行：快递员姓名 + 单号输入区 + 已识别数量'],
          ['添加快递员', '"+ 添加快递员"按钮，从该网点员工窗口列表中选择'],
          ['执行按钮', '"开始派件扫描（并发执行）"'],
          ['并发进度', '每个快递员独立进度条，实时更新'],
          ['完成汇总', '所有快递员完成后展示汇总报告'],
        ],
        [2000, 6600]
      ),
      blank(),
      infoBox('注意', '派件扫描要求对应员工的 EasyBR 窗口已登录，系统会在执行前检查窗口连接状态，未连接的快递员任务拒绝执行并提示'),
      blank(),

      h2('4.6  签收录入页面'),
      blank(),
      makeTable(
        ['区域', '内容'],
        [
          ['网点选择', '下拉框'],
          ['时间范围', '日期选择器，默认"今天"，支持自定义日期范围'],
          ['快递员选择', '多选 checkbox，列出该网点所有员工'],
          ['查询按钮', '"查询未签收运单"，显示每个快递员的未签收数量'],
          ['执行按钮', '"开始批量签收"'],
          ['执行结果', '每个快递员一行，显示签收成功数量或错误信息'],
        ],
        [2000, 6600]
      ),
      blank(),

      h2('4.7  操作日志页面'),
      blank(),
      makeTable(
        ['字段', '说明'],
        [
          ['任务类型', '到件 / 派件 / 签收'],
          ['网点', '天南大公司 / 和苑公司'],
          ['状态', '进行中 / 已完成 / 失败'],
          ['处理量', '成功 N 条 / 失败 M 条'],
          ['执行时间', '开始时间 ~ 结束时间'],
          ['操作', '查看详情（展开失败明细）/ 导出结果（CSV）'],
        ],
        [1800, 6800]
      ),
      blank(),
      rule(),

      // ══════════════════════════════════════════════════
      // 第五章  部署规格
      // ══════════════════════════════════════════════════
      h1('5  部署规格'),

      h2('5.1  目录结构'),
      blank(),
      code('bnsy-operator/'),
      code('├── src/                    # 后端源码（TypeScript）'),
      code('├── frontend/               # 前端源码（React + Vite）'),
      code('├── data/                   # SQLite 数据库文件（挂载到容器外）'),
      code('├── logs/                   # 操作日志和截图'),
      code('│   └── screenshots/        # 操作截图（用于排查问题）'),
      code('├── Dockerfile'),
      code('├── docker-compose.yml'),
      code('└── package.json'),
      blank(),

      h2('5.2  Dockerfile 要点'),
      bullet('基础镜像：node:20-slim'),
      bullet('安装 Playwright 及 Chromium 依赖（不需要 Chromium 本体，只用 CDP 连接本机 EasyBR）'),
      bullet('前端在构建阶段用 Vite 编译为静态文件，后端在运行时通过 Express 托管'),
      bullet('暴露端口 3100'),
      blank(),

      h2('5.3  docker-compose.yml 关键配置'),
      blank(),
      code('version: "3.8"'),
      code('services:'),
      code('  bnsy-operator:'),
      code('    build: .'),
      code('    ports:'),
      code('      - "3100:3100"'),
      code('    volumes:'),
      code('      - ./data:/app/data         # SQLite持久化'),
      code('      - ./logs:/app/logs         # 日志持久化'),
      code('    network_mode: host           # 关键：使用宿主机网络'),
      code('    restart: unless-stopped'),
      blank(),
      infoBox('network_mode: host 说明', '容器必须使用宿主机网络模式，才能访问本机 EasyBR 的 CDP 端口（127.0.0.1:XXXX）。如不配置此项，容器内无法连接到宿主机的 CDP 端口。'),
      blank(),

      h2('5.4  日常操作命令'),
      blank(),
      code('# 首次启动 / 重新构建'),
      code('docker-compose up -d --build'),
      blank(),
      code('# 日常启动（EasyBR 开启后执行）'),
      code('docker-compose up -d'),
      blank(),
      code('# 查看实时日志'),
      code('docker-compose logs -f'),
      blank(),
      code('# 停止服务'),
      code('docker-compose down'),
      blank(),
      code('# 访问界面'),
      code('浏览器打开：http://localhost:3100'),
      blank(),
      rule(),

      // ══════════════════════════════════════════════════
      // 第六章  开发阶段规划
      // ══════════════════════════════════════════════════
      h1('6  开发阶段规划'),

      h2('6.1  阶段拆分'),
      blank(),
      makeTable(
        ['阶段', '目标', '交付物', '验收标准'],
        [
          ['阶段一\n基础连通', 'BrowserPool 连接 EasyBR\n到件扫描跑通', 'BrowserPool.ts\nArriveScanBatch.ts\n前端到件页面\nDocker 可启动', '能对真实运单号完成一次批量到件扫描，结果正确显示'],
          ['阶段二\n派件', '多窗口并发派件', 'DispatchScan.ts\n前端派件页面', '两个员工窗口同时执行派件，各自单号互不干扰，进度实时更新'],
          ['阶段三\n签收', '签收录入自动化', 'SignReceipt.ts\n前端签收页面', '指定快递员和日期，自动查询并完成批量签收'],
          ['阶段四\n收尾', '日志、重试、完善', '操作日志页面\n断线重连\n结果导出 CSV', '历史任务可查询，失败单号可导出重试'],
        ],
        [1200, 2400, 2800, 2200]
      ),
      blank(),

      h2('6.2  技术约束'),
      bullet('TypeScript strict 模式，零 any，tsc 零错误'),
      bullet('中文注释'),
      bullet('Playwright 不使用 waitForTimeout，使用 waitForSelector / waitForResponse / waitForFunction'),
      bullet('每次操作前后截图保存到 ./logs/screenshots/，文件名含时间戳和操作类型'),
      bullet('操作结果以 JSONL 格式追加写入 ./logs/operations.jsonl'),
      bullet('任何操作失败不中断整体任务，记录失败原因继续处理下一条'),
      bullet('批次操作之间等待上一批系统响应，不盲目快速提交'),
      blank(),

      h2('6.3  待确认事项（开发前需验证）'),
      blank(),
      makeTable(
        ['编号', '事项', '影响模块', '确认方法'],
        [
          ['C-1', '批量到件 textarea 单次上限（是否有 500 条以上的限制）', '到件扫描分批逻辑', '手动测试：填入超过 500 条单号观察是否报错'],
          ['C-2', '派件扫描下拉框的 DOM 选择器（Element UI select 组件）', '派件扫描自动化', '开发者工具检查元素'],
          ['C-3', '签收页面"批量签收"按钮是否有二次确认弹窗', '签收录入逻辑', '手动操作一次观察'],
          ['C-4', 'toast 提示的具体 class 名称（.el-message 还是其他）', '所有操作结果读取', '开发者工具 Network / Elements 观察'],
        ],
        [700, 3200, 2000, 2700]
      ),
      blank(),
      rule(),

      // 尾部
      new Paragraph({
        alignment: AlignmentType.CENTER,
        spacing: { before: 400, after: 0 },
        children: [
          new TextRun({ text: '雨图饰品 · 内部工具文档  |  bnsy-operator v1.0  |  2026-06-18', size: 18, color: '9CA3AF', font: 'Arial' }),
        ],
      }),
    ],
  }],
});

Packer.toBuffer(doc).then(buf => {
  fs.writeFileSync('/mnt/user-data/outputs/bnsy-operator-spec-v1.0.docx', buf);
  console.log('done');
});
```
